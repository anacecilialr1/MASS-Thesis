\chapter{Models}



% The payoff is that single-band stops being a separate model and becomes the empty-support case of one model. "does multiband help?" result then becomes an ablation on a single trained network, varying the available bands at test time on the same curves, rather than a comparison between two differently-trained models, where any gap could be a training artifact.